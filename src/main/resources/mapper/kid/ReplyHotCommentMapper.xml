<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neu.dreambuilder.mapper.kid.ReplyHotCommentMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.neu.dreambuilder.entity.kid.ReplyHotComment">
        <result column="hot_id" property="hotId" />
        <result column="kid_id" property="kidId" />
        <result column="content" property="content" />
    </resultMap>

    <select id="selectAll" resultType="com.neu.dreambuilder.entity.kid.ReplyHotComment">
        select *
        from kid_reply_hot_comment
    </select>

</mapper>
